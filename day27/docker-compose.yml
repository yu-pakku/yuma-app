services:
  app:
    build: ./front
    volumes:
      - ./front:/front
    ports:
      - 3027:3000
  api:
    build: ./back
    volumes:
      - ./back:/back
    ports:
      - 8027:8000
    depends_on:
      - db
  db:
    image: mysql:8.4
    volumes:
      - ./back/mysql_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: dev
    ports:
      - 3327:3306

